# Copy to .env and fill in. Never commit .env.

# Studio-deployed Intelligent Contract. Empty = rehearsal mode.
PUBLIC_CONTRACT_ADDRESS=

# Optional. Rehearsal inspector only. Server-side. Never NEXT_PUBLIC_ / VITE_.
XAI_API_KEY=

# QA / local Python server
HOST=0.0.0.0
PORT=0
FORGE_DB=./data/forge_layer.sqlite
FORGE_STATIC=./frontend/out
